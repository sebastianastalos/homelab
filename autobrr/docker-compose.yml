services:
  autobrr:
    image: ghcr.io/autobrr/autobrr:v1.78.0
    container_name: autobrr
    user: "950:950"
    env_file: .env
    volumes:
      - /mnt/app/autobrr/config:/config
    ports:
      - "7474:7474"
    restart: unless-stopped
