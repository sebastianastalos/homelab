# Pinned so Renovate can track it, and so the baked-in runner matches what
# GitHub expects - the self-update can never persist here (see .env comment).
FROM myoung34/github-runner:2.336.0

RUN pip3 install websockets
