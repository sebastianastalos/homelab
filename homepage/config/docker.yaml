---
# For configuration options and examples, please see:
# https://gethomepage.dev/configs/docker/

# Disabled: container stats need a read-write docker.sock mount, which is
# root-equivalent on the host. The socket is currently mounted :ro.
# my-docker:
#   socket: /var/run/docker.sock
