---
services:
  trilium:
    image: triliumnext/trilium:v0.105.0
    container_name: trilium
    env_file:
      - .env
    volumes:
      - /mnt/app/trilium/data:/home/node/trilium-data
    ports:
      - 8081:8080
    mem_limit: 1g
    restart: unless-stopped
