TZ=Europe/London

# Public URL where aiostreams is reachable (include protocol + port if non-standard)
BASE_URL=http://<your-nas-ip>:8081

# 64-char hex string. Generate with: openssl rand -hex 32
SECRET_KEY=<generate-with-openssl-rand-hex-32>
