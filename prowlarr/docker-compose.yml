services:
  prowlarr:
    image: lscr.io/linuxserver/prowlarr:2.3.5
    container_name: prowlarr
    network_mode: "container:gluetun"
    env_file:
      - .env
    volumes:
      - /mnt/storage/prowlarr/config:/config
    restart: unless-stopped