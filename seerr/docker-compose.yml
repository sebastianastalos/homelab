services:
  seerr:
    image: ghcr.io/seerr-team/seerr:latest
    container_name: seerr
    init: true
    env_file:
      - .env
    volumes:
      - /mnt/storage/seerr/config:/app/config
    ports:
      - 5055:5055
    restart: unless-stopped