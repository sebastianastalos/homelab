services:
  adguard:
    image: adguard/adguardhome:v0.107.74
    container_name: adguard
    network_mode: host
    env_file:
      - .env
    volumes:
      - /mnt/app/adguard/conf:/opt/adguardhome/conf
      - /mnt/app/adguard/work:/opt/adguardhome/work
    restart: unless-stopped
