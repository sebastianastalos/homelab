services:
  profilarr:
    image: ghcr.io/dictionarry-hub/profilarr:2.0.9
    container_name: profilarr
    env_file:
      - .env
    ports:
      - "6868:6868"
    volumes:
      - /mnt/app/profilarr/config:/config
    restart: unless-stopped